package tech.hciot.dwis.business.application;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@SpringBootTest
@RunWith(SpringRunner.class)
public class WheelInStoreExportServiceTest {

  @Autowired
  WheelInStoreExportService wheelInStoreExportService;

  @Test
  public void computePageSumTest() {
//    Assert.assertEquals(wheelInStoreExportService.computePageSum(18), 1);
//    Assert.assertEquals(wheelInStoreExportService.computePageSum(19), 2);
//    Assert.assertEquals(wheelInStoreExportService.computePageSum(51), 2);
//    Assert.assertEquals(wheelInStoreExportService.computePageSum(52), 3);
//    Assert.assertEquals(wheelInStoreExportService.computePageSum(84), 3);
  }
}
